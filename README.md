## Wordpress Gutenberg ES6 React

ES6 BABEL Installation:

``` npm install --save-dev babel-preset-env

INSTALL BABEL CLI GLOBAL

``` npm install -g babel-cli

COMPILING CONFIG

``` "dev": "babel \"input.js\" \"-o\" \"output.js\""